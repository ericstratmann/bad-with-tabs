// Generated by CoffeeScript 1.3.3
(function() {
  var readLocal, saveLocal;

  $(function() {
    var policy;
    policy = readLocal();
    if (policy) {
      $("#max_tabs_per_window").val(policy["maxTabsPerWindow"]);
    }
    return $("#options_form").submit(function() {
      var maxTabsPerWindow;
      policy = {};
      maxTabsPerWindow = $("#max_tabs_per_window");
      policy["maxTabsPerWindow"] = maxTabsPerWindow.val();
      saveLocal(policy);
      return false;
    });
  });

  readLocal = function() {
    if (localStorage["policy"]) {
      return JSON.parse(localStorage["policy"]);
    }
  };

  saveLocal = function(policy) {
    return localStorage["policy"] = JSON.stringify(policy);
  };

}).call(this);
